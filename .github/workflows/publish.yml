name: 🚀 Publish Site
on:
  push:
    branches-ignore:
      - main
# on:
#   push:
#     branches:
#       - main
jobs:
  build:
    name: Build Site
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install Pubcrank
        uses: threeal/pipx-install-action
        with:
          packages: pubcrank-cli
      - name: Build site
        run: pubcrank build

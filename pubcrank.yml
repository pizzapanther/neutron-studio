editable:
  - content
uploads:
  - dir: static
    url: ""
template_field: template
template_default: single
templates:
  single:
    fields:
      title:
        attrs:
          maxlength: 70
      description:
        attrs:
          maxlength: 255
      date:
        type: datetime
      draft:
        type: boolean
  post:
    fields:
      title:
        attrs:
          maxlength: 70
      description:
        attrs:
          maxlength: 255
      date:
        type: datetime
      draft:
        type: boolean
      categories:
        type: list[text]
      cover:
        type: image


